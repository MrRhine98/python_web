# ----Pool-----
import multiprocessing

def main():
    # pool = multiprocessing.Pool(3)
    # pool.apply_async(func, (args, ))
    # pool.close()
    # pool.join()   # wait for the sub process to finish

if __name__ == "__main__":
    main()
